//
//  constrainsProtocol.swift
//  constrains
//
//  Created by Hazel Alain on 20/10/24.
//  
//

import Foundation
import UIKit
/// Protocolo que define los métodos y atributos para el view de constrains
protocol constrainsViewProtocol {
    // PRESENTER -> VIEW
}
/// Protocolo que define los métodos y atributos para el routing de constrains
protocol constrainsRouterProtocol {
    // PRESENTER -> ROUTING
}
/// Protocolo que define los métodos y atributos para el Presenter de constrains
protocol constrainsPresenterProtocol {
    // VIEW -> PRESENTER
}
/// Protocolo que define los métodos y atributos para el Interactor de constrains
protocol constrainsInteractorInputProtocol {
    // PRESENTER -> INTERACTOR
}
/// Protocolo que define los métodos y atributos para el Interactor de constrains
protocol constrainsInteractorOutputProtocol {
    // INTERACTOR -> PRESENTER
}

